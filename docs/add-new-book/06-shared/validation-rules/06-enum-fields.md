# Enum fields

- Enum values must match canonical config.
- Unknown reading status, ownership status, format, genre, language or age category is invalid.
- Do not display technical statuses as manual options if they are computed/action-only.
