# Required fields

- Title is required.
- Author is required.
- Reading status is required but defaults to `not_started`.
- Ownership status is required but defaults to `none`.
- Book type is required but defaults to `solo`.
- Series name and part number are required only when book type is `series_part`.
