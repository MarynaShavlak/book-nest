# MVP vs Future scope

- MVP: create/edit book, basic fields, statuses, series relation, favorites, queue, custom lists and cover.
- MVP can hide rereading if repeat-reading logic is not ready.
- Future: advanced ISBN lookup, external metadata import, public profiles, custom genres and richer cover processing.
