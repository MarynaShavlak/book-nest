# Edit book action

- Updates an existing book record.
- Initial values come from the saved book.
- Optional fields can be cleared.
- Existing custom entities should not be deleted just because a book no longer uses them.
