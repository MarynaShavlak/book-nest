# Create book initial values

- Empty title, author, publisher and description.
- readingStatus defaults to `not_started`.
- ownershipStatus defaults to `none`.
- bookType defaults to `solo`.
- formats, genres, tags, custom lists are empty.
