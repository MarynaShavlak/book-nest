# Cover storage contract

- Cover file is optional.
- Only JPG, PNG and WEBP are allowed.
- Create mode should avoid uploading if the user cancels the form.
- Existing cover should remain safe if replace/remove save fails.
