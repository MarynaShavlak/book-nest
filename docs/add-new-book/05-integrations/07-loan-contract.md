# Loan contract

- Ownership `borrowed_from_someone` and `lent_to_someone` show loan conditional fields.
- Person name is required for active loan statuses.
- Due statuses are computed, not manually selected.
- Returned flow should be an action outside basic ownership selection.
