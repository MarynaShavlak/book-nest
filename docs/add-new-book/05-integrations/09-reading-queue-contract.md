# Reading Queue contract

- `want_to_read` does not automatically add to queue.
- Queue addition is controlled by a separate checkbox/switch.
- Queue priority only matters if add-to-queue is enabled.
- Queue item is created after book submit.
