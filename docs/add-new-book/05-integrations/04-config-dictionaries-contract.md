# Config dictionaries contract

- Genres, age categories, languages, reading statuses, ownership statuses, formats, delivery statuses and series statuses must come from canonical config.
- Unknown values are invalid.
- UI labels/icons/tones should not be hardcoded in every component.
