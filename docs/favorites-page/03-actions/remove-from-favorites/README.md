# Remove From Favorites

Це page-level дія для сторінки Favorites.

Вона використовує той самий `isFavorite` toggle, але має специфічну поведінку: книга зникає зі сторінки Favorites і може бути повернута через Undo toast.
