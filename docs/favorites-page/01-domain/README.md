# Domain Rules

Ця папка описує бізнес-логіку Favorites module без привʼязки до конкретної сторінки.

Починай звідси, якщо треба зрозуміти, що таке `isFavorite`, які книги потрапляють у Favorites, які правила доступу, що оновлюється після toggle і що не має змінюватись.
