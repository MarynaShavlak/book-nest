# Custom Lists Page

Route:

```text
/lists
```

Purpose:

```text
Show all custom lists of the current user.
```

Use this folder when implementing the page shell, toolbar, cards, sidebar, and states.
