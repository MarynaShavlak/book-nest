# Add Book to Custom Lists from Book Details

This action is used from Book Details.

It lets the user add one current book to one or multiple existing custom lists and optionally create a new list inside the modal.
