# Domain Rules

Use these files as the stable source of truth for the Custom Lists module.

The domain layer answers:

- what a custom list is;
- which fields it has;
- how books are connected to lists;
- how duplicates are prevented;
- what happens when a list is deleted;
- what validation and permissions are required.
