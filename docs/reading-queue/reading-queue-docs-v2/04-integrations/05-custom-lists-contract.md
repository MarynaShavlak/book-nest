# Custom Lists Contract

## Independence rule

Adding/removing/reordering Reading Queue must not change Custom Lists.

A book can be:

- in one or many custom lists;
- in Reading Queue;
- both at the same time.

## Possible UI relation

If Custom List cards show queue badge, they should update after queue changes.

No custom list membership should be changed.
