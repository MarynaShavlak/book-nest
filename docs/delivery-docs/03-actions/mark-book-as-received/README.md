# Action: Mark Book as Received

Purpose:

```text
Move active delivery to history and set book ownership to owned.
```

Supports:

- single record action;
- bulk selected action;
- bulk all visible/filtered active deliveries action.
