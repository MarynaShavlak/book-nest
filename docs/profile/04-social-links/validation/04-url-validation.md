# URL Validation

## Rules

- optional if username is filled;
- must be valid URL;
- only `https://` allowed;
- disallow `javascript:`, `data:`, `file:`;
- max 300 chars;
- should match selected platform when possible.

## Error

```text
Введіть коректне https-посилання.
```
