# Duplicate Prevention

## Platform duplicate

Same platform can be added only once.

Exception:

- `OTHER` can be added multiple times.

## URL duplicate

Same URL cannot be added more than once.

## Username duplicate

If platform + normalized username already exists, show validation error.

## Error

Use clear field-level or form-level error.
